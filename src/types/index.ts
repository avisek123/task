export type {
  PublicStackParams,
  PublicNavigationProps,
  RootRouteProps,
  PrivateStackParams,
  PrivateRootRouteProps,
} from './allRoutes';
export type ClassType = {
  subject: string;
  time: string;
  color: string;
};
