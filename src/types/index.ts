export type {
  PublicStackParams,
  PublicNavigationProps,
  RootRouteProps,
  PrivateStackParams,
  PrivateRootRouteProps,
} from './allRoutes';
export type FirebaseErrors = {
  [key: string]: string;
};
